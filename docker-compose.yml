version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ENV=development
      - PORT=8080
      - HOST=0.0.0.0
      - DSN=${DSN}
      - JWT_SECRET=${JWT_SECRET}
      - API_KEY=${API_KEY}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS}
      - OPEN_AI_API_KEY=${OPEN_AI_API_KEY}
      - ELEVEN_LABS_API_KEY=${ELEVEN_LABS_API_KEY}
      - UNREAL_SPEECH_API_KEY=${UNREAL_SPEECH_API_KEY}
      - VERTEX_AI_API_KEY=${VERTEX_AI_API_KEY}
      - GCLOUD_API_KEY=${GCLOUD_API_KEY}
      - PINECONE_API_KEY=${PINECONE_API_KEY}
      - PINECONE_CONNECTION=${PINECONE_CONNECTION}
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Uncomment to add MySQL database
  # mysql:
  #   image: mysql:8.0
  #   environment:
  #     MYSQL_ROOT_PASSWORD: rootpassword
  #     MYSQL_DATABASE: interview_trainer
  #     MYSQL_USER: appuser
  #     MYSQL_PASSWORD: apppassword
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - mysql_data:/var/lib/mysql
  #   healthcheck:
  #     test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

# volumes:
#   mysql_data:

